@model IEnumerable<Horizone.Models.PreparationPaperBeforeUsing>

@{
    ViewBag.Title = @StaticResource.Resources.PreparationPaperBeforeUsing;
}

<h2>@StaticResource.Resources.PreparationPaperBeforeUsing </h2>

<p>
    @Html.ActionLink(@StaticResource.Resources.Add, "Create", null, new { @class = "btn btn-primary" })
</p>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PreparationEn) @StaticResource.Resources.English
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PreparationFr) @StaticResource.Resources.French
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PreparationZh) @StaticResource.Resources.Chinese
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PreparationEn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PreparationFr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PreparationZh)
            </td>
            <td>
                @Html.ActionLink(@StaticResource.Resources.Edit, "Edit", new { id = item.Id }, new { @class = "btn btn-info", style = "width:85px" })
                 @Html.ActionLink(@StaticResource.Resources.Delete, "Delete", new { id = item.Id }, new { @class = "btn btn-info", style = "width:85px" })
            </td>
        </tr>
    }

</table>
